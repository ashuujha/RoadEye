# Execution record

1. Inspect and specify: empty existing Git repository, no handbook or AGENTS.md found; preserve protected configuration directories. Dedicated local branch.
2. Database, sessions and atomic ingestion.
3. Curated fixtures, evidence, counting and consensus.
4. Bounded graph reconstruction and immutable query versions.
5. SQL aggregates, watchlist lifecycle, reviews and audit.
6. Connected React console and generated OpenAPI types.
7. Real PostgreSQL integration, browser, replay and recovery tests.
8. Critical review, corrections and measured validation documentation.

Important decisions: one fictional network; complete OCR batches per passage; immutable raw input; review as append-only revision; synthetic event clock for health/replay; unique event delivery plus payload-conflict checks; bounded non-greedy link alternatives. Tests must challenge concurrency, authorization, crash windows and source isolation. No requirement is considered delivered merely because it appears here.

## Phase results

1. Specification/architecture recorded and checkpointed; handbook absent.
2–5. Backend implemented; PostgreSQL scenario, concurrency, recovery, authorization and version tests executed.
6. Console connected to generated API client; frontend types/build passed.
7. Actual API/worker/PostgreSQL process E2E and Chromium checks passed; full database restart preserved prior run effects.
8. Critical review fixes and final full validation executed; measured report records exact native environment and Docker/hosted-CI limitations.
